
import React from 'react';
import ReactDOM from 'react-dom/client';
import ConvertaVoxApp from './ConvertaVoxApp';

const root = document.getElementById('root');
if (root) {
  ReactDOM.createRoot(root).render(<ConvertaVoxApp />);
}
